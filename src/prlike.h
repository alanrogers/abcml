/** prlike.h: Prototypes for functions in prlike.c  **/

void ltransect(int k, real *p, int len, real l, real h, int n);
void all_ltransects(real *p, int len, int n, real lkappa, real hkappa,
		    real lbeta, real hbeta);
